/**
 * 游客（未登录）翻译限流配置 —— 集中管理，可调
 * 背景：审计 P0 —— 游客免费翻译无限制直连付费 API（成本漏洞）
 * 策略：频率（内存滑动窗口）+ 日预算（DB 持久化，重启不绕过）
 */
export const GUEST_LIMITS = {
  /** 频率窗口：毫秒 */
  windowMs: 60_000,
  /** 窗口内最大请求次数（真实 API 调用，缓存命中不计） */
  freqMax: 15,
  /** 每日最大成功翻译次数（按 IP） */
  dailyMax: 30,
  /** 每日最大翻译字符量（按 IP） */
  dailyChars: 5_000,
} as const;
